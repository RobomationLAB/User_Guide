---
title: Piobot
---

# Piobot

## Объявление экземпляра {#instance}

После добавления блока Piobot(Pio) в рабочую область в код Python автоматически вставляется следующее объявление экземпляра:

```python
pio = Pio(0)
# Когда экземпляров несколько
pio_1 = Pio(1)
```

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| index | Пункт выпадающего списка | Номер экземпляра (начиная с 0) | Целое число 0 или больше | 0 |


## Установить скорость колеса {#set_wheel_speed}

Задаёт скорость колеса. Диапазон скорости колеса — от -100 до 100.

<BlockImage module="roboids/Pio" id="set_wheel_speed" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| unit | Пункт выпадающего списка | Выбор колеса | левое(left), правое(right), оба(both) | - |
| speed | Ввод (блок) | Скорость колеса | Целое число от -100 до 100, 0: остановка | - |

### Python
```python
pio = Pio(0)

pio.set_wheel_speed('both', 50)
```

## Двигаться на расстояние {#move_distance}

Едет с текущей скоростью колёс на заданное расстояние.  
Если скорость колёс не задана, робот едет вперёд со скоростью по умолчанию.  
Если значение расстояния равно 0, робот продолжает ехать с текущей скоростью колёс.  
Если установлен флажок ждать, программа ждёт завершения движения.

<BlockImage module="roboids/Pio" id="move_distance" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| data | Ввод (блок) | Пройденное расстояние | Вещественное число 0 или больше | - |
| unit | Пункт выпадающего списка | Единица расстояния | см(cm), мм(mm), дюйм(inch) | cm |
| wait | Флажок | Ждать ли завершения | TRUE / FALSE | TRUE |

### Python
```python
pio = Pio(0)

pio.move_distance(50, 'cm', wait=True)
```

## Двигаться в течение времени {#move_time}

Едет с текущей скоростью колёс заданное время.  
Если скорость колёс не задана, робот едет вперёд со скоростью по умолчанию.  
Если установлен флажок ждать, программа ждёт завершения движения.

<BlockImage module="roboids/Pio" id="move_time" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| data | Ввод (блок) | Время движения (в секундах) | Вещественное число 0 или больше | - |
| wait | Флажок | Ждать ли завершения | TRUE / FALSE | TRUE |

### Python
```python
pio = Pio(0)

# wait = TRUE
pio.move_time(5, wait=True)
# wait = FALSE
pio.move_time(5, wait=False)
```

## Повернуть на месте {#turn_degree}

Задаёт направление и угол поворота на месте.  
Если установлен флажок ждать, программа ждёт завершения поворота.

<BlockImage module="roboids/Pio" id="turn_degree" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| direction | Пункт выпадающего списка | Направление поворота | слева(left), справа(right) | - |
| data | Ввод (блок) | Угол поворота (в градусах) | Вещественное число 0 или больше | - |
| wait | Флажок | Ждать ли завершения | TRUE / FALSE | TRUE |

### Python
```python
pio = Pio(0)

# direction = "left"
pio.turn_degree('left', 90, wait=True)
# direction = "right"
pio.turn_degree('right', 90, wait=True)
```

## Изменить скорость колеса {#change_speed}

Изменяет скорость колёс робота Piobot.  
Новая скорость колеса — это текущая скорость плюс введённое значение.  
Новая заданная скорость колеса ограничивается диапазоном от -100 до 100.

<BlockImage module="roboids/Pio" id="change_speed" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| unit | Пункт выпадающего списка | Выбор колеса | левое(left), правое(right), оба(both) | - |
| speed | Ввод (блок) | Изменение скорости | Целое число от -200 до 200 | - |

### Python
```python
pio = Pio(0)

pio.change_wheel_speed('both', 50)
```

## Включить / выключить турборежим {#turbo}

Включает или выключает турборежим робота Piobot.

<BlockImage module="roboids/Pio" id="turbo" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| unit | Пункт выпадающего списка | Турборежим ON / OFF | вкл.(on=True), выкл.(off=False) | TRUE |

### Python
```python
pio = Pio(0)

pio.turbo(True)
```

## Остановиться {#stop}

Останавливает движение робота Piobot.  
Скорость обоих колёс робота Piobot обнуляется.

<BlockImage module="roboids/Pio" id="stop" />

### Параметры

(нет)

### Python
```python
pio = Pio(0)

pio.stop()
```

## Колесо движется? {#wheel_moving}

Возвращает true, если колесо движется, и false, если оно стоит.

<BlockImage module="roboids/Pio" id="wheel_moving" />

### Параметры

(нет)

### Python
```python
pio = Pio(0)

pio.wheel_moving()
```

## Переместиться на один шаг по полю {#grid_move}

Перемещается по полю на один заданный шаг.

<BlockImage module="roboids/Pio" id="grid_move" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| unit | Пункт выпадающего списка | Направление движения | вперёд(forward), назад(backward), влево(left), вправо(right) | - |

### Python
```python
pio = Pio(0)

pio.grid_move('forward')
```

## Повернуть один раз на поле {#grid_turn}

Piobot поворачивается на поле на 90 градусов в заданном направлении. Всегда ждёт завершения (внутри wait=True задано жёстко).

<BlockImage module="roboids/Pio" id="grid_turn" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| unit | Пункт выпадающего списка | Направление поворота | слева(left), справа(right) | - |

### Python
```python
pio = Pio(0)

# unit = "left"
pio.grid_turn('left')
# unit = "right"
pio.grid_turn('right')
```

## Установить скорость шеи {#set_neck_speed}

Задаёт скорость поворота шеи. Диапазон скорости шеи — от 1 до 6.

<BlockImage module="roboids/Pio" id="set_neck_speed" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| data | Ввод (блок) | Скорость поворота шеи | Целое число от 1 до 6 | 4 |

### Python
```python
pio = Pio(0)

pio.set_neck_speed(4)
```

## Установить угол шеи {#set_neck_angle}

Задаёт угол, до которого повернётся шея. Диапазон угла шеи — от -45 до 45.

<BlockImage module="roboids/Pio" id="set_neck_angle" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| data | Ввод (блок) | Угол шеи (в градусах) | Вещественное число от -45 до 45 | - |
| wait | Флажок | Ждать ли завершения | TRUE / FALSE | TRUE |

### Python
```python
pio = Pio(0)

pio.set_neck_angle(15, wait=True)
```

## Шея движется? {#neck_moving}

Возвращает true, если шея движется, и false, если она стоит.

<BlockImage module="roboids/Pio" id="neck_moving" />

### Параметры

(нет)

### Python
```python
pio = Pio(0)

pio.neck_moving()
```

## Установить цвет глаза {#set_eye_color}

Задаёт цвет светодиода в глазу робота Piobot.  
Можно изменить цвет левого глаза, правого глаза или обоих сразу.  
Цвет, выбранный среди готовых цветов, превращается в **название цвета** (текст на английском) и в таком виде попадает в код. (В коде появляется название цвета, а не числовые значения R, G, B.)

<BlockImage module="roboids/Pio" id="set_eye_color" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| unit | Пункт выпадающего списка | Выбор глаза | левый(left), правый(right), оба(both) | - |
| color | Пункт выпадающего списка | Готовый цвет → превращение в название цвета (на английском) | чёрный(black), красный(red), жёлтый(yellow), зелёный(green), голубой(cyan), синий(blue), пурпурный(magenta), белый(white) | - |

### Python
```python
pio = Pio(0)

pio.set_eye_color('both', 'red')
```

## Установить цвет глаза блоком из категории Цвет {#set_eye_color_with_block}

Задаёт цвет светодиода в глазу робота Piobot с помощью блоков из категории Цвет.  
Можно изменить цвет левого глаза, правого глаза или обоих сразу.

<BlockImage module="roboids/Pio" id="set_eye_color_with_block" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| unit | Пункт выпадающего списка | Выбор глаза | левый(left), правый(right), оба(both) | - |
| data | Ввод (цвет) | Массив [R, G, B] | Блок из категории Цвет или `[0~255, 0~255, 0~255]` | - |

### Python
```python
pio = Pio(0)

pio.set_eye_color('both', *Utils.color('red'))
```

## Изменить цвет глаза на RGB {#change_by_rgb}

Изменяет цвет светодиода в глазу робота Piobot на заданные значения R, G и B.  
Можно задать цвет левого глаза, правого глаза или обоих сразу.

<BlockImage module="roboids/Pio" id="change_by_rgb" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| unit | Пункт выпадающего списка | Выбор глаза | левый(left), правый(right), оба(both) | - |
| r | Ввод (поле) | Изменение красного | Целое число от -255 до 255 | 0 |
| g | Ввод (поле) | Изменение зелёного | Целое число от -255 до 255 | 0 |
| b | Ввод (поле) | Изменение синего | Целое число от -255 до 255 | 0 |

### Python
```python
pio = Pio(0)

pio.change_eye_color('both', 10, 0, 0)
```

## Установить узор глаз {#set_eye_pattern}

Задаёт узор глаз и цвет каждого глаза в момент начала узора.

<BlockImage module="roboids/Pio" id="set_eye_pattern" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| pattern | Пункт выпадающего списка | Вид узора | выключить(reset), мигание(blink), затухание(dimming), радуга(rainbow) | - |
| left | Пункт выпадающего списка | Цвет левого глаза | по умолчанию(black), красный(red), жёлтый(yellow), зелёный(green), голубой(cyan), синий(blue), пурпурный(magenta), белый(white) | white |
| right | Пункт выпадающего списка | Цвет правого глаза | (так же, как left) | white |

### Python
```python
pio = Pio(0)

pio.set_eye_pattern('dimming', 'green', 'red')
```

## Выключить глаз {#turn_off}

Гасит цвет глаза.

<BlockImage module="roboids/Pio" id="turn_off" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| unit | Пункт выпадающего списка | Выбор глаза | левый(left), правый(right), оба(both) | both |

### Python
```python
pio = Pio(0)

pio.turn_off('both')
```

## Установить частоту зуммера {#sound_buzz}

Задаёт звук зуммера робота Piobot с указанной частотой.  
Диапазон частот, которые робот может издать, — от 27,5 Гц до 6553,5 Гц.  
При вводе значения вне этого диапазона зуммер не издаёт звука.

<BlockImage module="roboids/Pio" id="sound_buzz" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| hz | Ввод (блок) | Частота (Гц) | Вещественное число от 27,5 до 6553,5 | - |

### Python
```python
pio = Pio(0)

pio.sound_buzz(440)
```

## Играть ноту {#sound_note}

Piobot играет заданную ноту.

<BlockImage module="roboids/Pio" id="sound_note" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| note | Пункт выпадающего списка | Нота | До(C), До#/Ре♭(C#), Ре(D), Ре#/Ми♭(D#), Ми(E), Фа(F), Фа#/Соль♭(F#), Соль(G), Соль#/Ля♭(G#), Ля(A), Ля#/Си♭(A#), Си(B) | - |
| octave | Пункт выпадающего списка | Октава | 1, 2, 3, 4, 5, 6, 7 | 4 |

### Python
```python
pio = Pio(0)

pio.sound_note('D', 5)
```

## Воспроизвести звук {#sound_clip}

Piobot воспроизводит выбранный звуковой клип.  
Если установлен флажок ждать, программа ждёт завершения воспроизведения.

<BlockImage module="roboids/Pio" id="sound_clip" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| clip | Пункт выпадающего списка | Название звукового клипа | `'mute'`, `'beep'`, `'beep2'`, `'beep3'`, `'siren'`, `'engine'`, `'robot'`, `'connect'` и другие | - |
| wait | Флажок | Ждать ли завершения | TRUE / FALSE | TRUE |

### Python
```python
pio = Pio(0)

pio.sound_clip('siren', wait=True)
```

## Воспроизвести мелодию {#sound_melody}

Piobot воспроизводит выбранную мелодию.  
Если установлен флажок ждать, программа ждёт завершения воспроизведения.

<BlockImage module="roboids/Pio" id="sound_melody" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| melody | Пункт выпадающего списка | Название мелодии | `'mute'`, `'happy'`, `'angry'`, `'sad'`, `'sleep'`, `'march'`, `'birthday'`, `'dibidibidip'`, `'good_job'` и другие | - |
| wait | Флажок | Ждать ли завершения | TRUE / FALSE | TRUE |

### Python
```python
pio = Pio(0)

pio.sound_melody('happy', wait=True)
```

## Выключить звук {#sound_off}

Выключает звук робота Piobot.

<BlockImage module="roboids/Pio" id="sound_off" />

### Параметры

(нет)

### Python
```python
pio = Pio(0)

pio.sound_off()
```

## Звук воспроизводится? {#sound_playing}

Возвращает true, если звук воспроизводится, и false, если нет.

<BlockImage module="roboids/Pio" id="sound_playing" />

### Параметры

(нет)

### Python
```python
pio = Pio(0)

pio.sound_playing()
```

## Скорость колеса {#wheel_speed}

Скорость выбранного колеса

<BlockImage module="roboids/Pio" id="wheel_speed" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| unit | Пункт выпадающего списка | Считываемое колесо | слева(left), справа(right) | - |

### Python
```python
pio = Pio(0)

pio.wheel_speed('left')
```

## Уровень сигнала {#signal_strength}

Уровень сигнала

<BlockImage module="roboids/Pio" id="signal_strength" />

### Параметры

(нет)

### Python
```python
pio = Pio(0)

pio.signal_strength()
```

## Напряжение батареи {#battery}

Напряжение батареи

<BlockImage module="roboids/Pio" id="battery" />

### Параметры

(нет)

### Python
```python
pio = Pio(0)

pio.battery()
```

## Кнопка клавиатуры {#keypad}

Определяет кнопку клавиатуры, нажатую пользователем последней.

<BlockImage module="roboids/Pio" id="keypad" />

### Параметры

| Название | Вид | Описание | Диапазон / Тип | По умолчанию |
| --- | --- | --- | --- | --- |
| button | Пункт выпадающего списка | Определяемая кнопка | запустить(play), двигаться вперёд(forward), двигаться назад(backward), двигаться влево(left), двигаться вправо(right), выполнить(action), повторить(repeat), удалить(clear) | - |

### Python
```python
pio = Pio(0)

pio.keypad('forward')
```
